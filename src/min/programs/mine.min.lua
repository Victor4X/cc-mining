require(settings.get("ghu.base").."core/apis/ghu")require("miningLib")function init()res,items=sortReqs()if not res then print("Missing these items:")for a,b in pairs(missing)do print(a)end;print("Trying to find them in the surroundings...")res=mineByName(items,1,true)if res then return select(1,sortReqs())end end;return true end;print("Initializing...")if init()then print("Successfully Initialized!")else error("Init failed")end;function checkFuel()if t.getFuelLevel()<t.getFuelLimit()*0.5 then refuel()end end;function checkInv()if getNumEmpty()<4 then dumpInv()end end;function checkInvAndFuel()checkInv()checkFuel()end;function digWall5()mineWall(2)end;chain(500,digWall5,checkInvAndFuel,t.dig,t.forward)chain(500,t.back)
